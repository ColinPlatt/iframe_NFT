<svg 
    xmlns="http://www.w3.org/2000/svg"  
    xmlns:xlink="http://www.w3.org/1999/xlink" 
    version="1.1" viewBox="0 0 350 350" height="350" width="350"
    >
    <rect width="100%" height="100%" fill="gray" />
    <g onclick="connection(evt)"> 
        <rect class="btn" x="100" y="50" width="150" height="50" fill="lightGray" stroke="black" />
        <text x="100" y="50" font-size="20" fill="black" text-anchor="middle" alignment-baseline="middle">
            <tspan x="175" y="75">Click me!</tspan>
        </text>
    </g>

    <script xlink:href="https://cdnjs.cloudflare.com/ajax/libs/ethers/6.2.3/ethers.umd.min.js" type="text/javascript"></script>
    <script type="text/javascript" crossorigin="anonymous" >   

            async function connection(evt) {
                console.log(window.location.href)
                
            } 
        
    </script>
    

</svg>
<!--window.ethereum.send('eth_sendTransaction', { from: accounts[0] })   

let signer = null;
                let provider;
                if (window.ethereum == null) {
                    console.log("MetaMask not installed; using read-only defaults")
                    provider = ethers.getDefaultProvider()

                } else {
                    provider = new ethers.BrowserProvider(window.ethereum)
                    signer = await provider.getSigner();
                    console.log(signer.address);
                }

-->